defaults:
  - train_transform: augment
  - test_transform: simple

name: IPA
num_classes: 37
batch_size: 64
num_workers: 8
train_path: ${data_dir}/train/IPA_merge
# train_path: ${data_dir}/val
real_images_val_path: ${data_dir}/val
test_path: ${data_dir}/test 
